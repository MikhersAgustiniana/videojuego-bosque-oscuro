// Verifica si la tecla de flecha izquierda está presionada
if keyboard_check(vk_left) {
    // Define la velocidad a la que se moverán los objetos hacia la derecha
    var velocidad = 1; // Puedes ajustar este valor según tus necesidades
    
    // Mueve el objeto actual hacia la derecha
    x += velocidad;
}
